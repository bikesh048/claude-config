# What gets installed by default when running: ./setup.sh <project-path>
# Use "all" to install everything in that category, or list specific files.
# Empty array = skip that category.

COMMANDS=(all)

RULES=()

AGENTS=()

SKILLS=()

TEMPLATES=(all)

# Install shared settings.json? Set to false to keep your own.
INSTALL_SETTINGS=false
